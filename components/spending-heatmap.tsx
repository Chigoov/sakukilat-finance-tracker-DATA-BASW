// Deprecated: SpendingHeatmap was removed per UX feedback (desktop layout broke).
// Kept as a no-op stub to preserve import history; safe to delete the file
// entirely in a follow-up cleanup PR.
export function SpendingHeatmap() {
  return null
}
